import { strings } from '../strings.pl'

export function LegChipCountPage() {
  return (
    <main className="mx-auto max-w-md p-4">
      <h1 className="mb-4 text-2xl font-semibold">{strings.chipCount.title}</h1>
      <p className="text-slate-500">TODO: Phase 5 — chip count entry, reconciliation hard-block.</p>
    </main>
  )
}
